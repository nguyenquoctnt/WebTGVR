<header class="header">
    <a href="/" class="logo">
    </a>
    <nav class="navbar navbar-static-top" role="navigation">
        <!-- Sidebar toggle button-->
        <a href="#" class="navbar-btn sidebar-toggle" data-toggle="offcanvas" role="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </a>
        <div class="navbar-right">
            <ul class="nav navbar-nav">
                <li class="dropdown user user-menu">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <i class="glyphicon glyphicon-user"></i>
                        @*<span>@ViewBag.LogedName <i class="caret"></i></span>*@
                    </a>
                    <ul class="dropdown-menu" style="margin: 1px 0px">
                        <!-- User image -->
                        <!-- Menu Body -->
                        <!-- Menu Footer-->
                        <li class="user-footer">
                            <div class="pull-left">
                                <a href="@Url.Action("ProfileUser", "Account")" class="btn btn-default btn-flat">Thông tin tài khoản</a>
                            </div>
                            <div class="pull-right" style="padding-top:10px">
                                @if (Request.IsAuthenticated)
                                {
                                    <a href="@Url.Action("LogOff", "Account")" style="color:red">Đăng xuất</a>
                                }
                                else
                                {
                                    <a href="@Url.Action("Login", "Account")" style="color:red"> Đăng nhập</a>
                                }
                            </div>
                        </li>

                    </ul>
                </li>
            </ul>
        </div>

    </nav>
</header>